# Working manual candidates

Khartes exports go here first as `<scroll>_umbilicus.candidate.json`. They are
not release curves. After fresh-project reimport and human visual QC, run
`approve_manual_curve.py`; only that tool may create `manual/<scroll>_umbilicus.json`
and its hash-bound QC manifest.
